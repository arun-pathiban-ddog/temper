# Contract

Trusted receiving-repository workflows may fetch private Stack tooling. Contributor code is never checked out or executed with secrets. Validation reads contribution Git objects and records results on its actual current commit. Evidence reruns locate that check’s workflow. Existing human-approved arbitration after three rounds is represented honestly; ordinary review requirements remain unchanged.

No MCP, proof-submission API, review replacement chains, backend detection, or Genesis changes.
